<!--
PLAN — Build stage.
Produced by Claude Code in plan mode (read-only) against an approved spec.md. Review and
approve this BEFORE Claude writes any code. Save next to the spec:
docs/ai-native-sdlc/intent/YYYY-MM-DD-short-slug.plan.md
-->

# Plan: <short title>

**From spec:** `docs/ai-native-sdlc/intent/YYYY-MM-DD-short-slug.spec.md`
**Date:** YYYY-MM-DD
**Status:** proposed | approved | implemented

## Implementation steps
Ordered list of concrete changes — files/modules touched and what happens to each.

1. ...
2. ...

## Test plan
- Unit/integration tests to add or update (`turbo test`)
- Visual diff needed? (`node tools/visual-diff.mjs`) — required for any `apps/web` UI change
- Manual verification needed (e.g. real-iPhone check for background audio)?

## Risks / rollback
What could go wrong, and how to back out if it does.

## Approval
- [ ] Reviewed by Roger — Claude may implement
